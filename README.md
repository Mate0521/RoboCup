# RoboCup
# Comandos Docker

Armar hoja: docker compose up --build

Cargar jugadores:
1- docker compose down team_left
2- docker compose build team_left
3- Volver armar la hoja: docker compose up --build


Servidor: TigerVNC - Servidor VNC:localhost:5900 (Conectar)